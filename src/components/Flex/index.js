import styled from 'styled-components';
import {
  space,
  width,
  fontSize,
  minHeight,
  height,
} from 'styled-system';

const Flex = styled.div`
  display: flex;
  ${space}
  ${width}
  ${fontSize}
  ${height}
  ${minHeight}
`;

export default Flex;
